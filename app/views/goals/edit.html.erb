<% content_for :body_type do%>
goalsedit<% end %>

<h3 id="editgoalheader">Edit your goal</h3>

<div class="editgoalform">
	<div class="col-md-6 col-sm-6 col-xs-6 col-md-offset-2 col-sm-offset-2">
		<%= bootstrap_form_for @goal do |f| %>

		<%= f.text_field :name  %></br>
	
		<%= f.text_area :description %></br>
		<div id="editgoalsubmit">
		<%= f.submit "Update", class: "btn btn-primary" %>
		</div>
	</div>
		<%end%>
</div>